p1b Worksheet

a. What is the value of label Z?

b. What is the value of the data referenced by the label Y? (hex)

c. What is the value of the data referenced by the label Y? (decimal)

d. What memory address is referenced by the label Y? (hex)

e. What memory address is referenced by the label X?

f. What is the machine instruction at location 0x8 of the .text section? 

g. What is the offset value of the instruction at location 0x14 of the .text section?

h. What will the contents of register r7 be at completion of execution (hex)?

i. Screenshot the mem (bytes) for the data section bounded by the begin and end coremarks. Circle and identify the bytes for variables X, Y and Z (step thru
program first)